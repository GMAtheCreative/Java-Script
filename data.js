let id = "1";
let available = "true";
let count = "6"
let name = "Mystery";
let author = "Miracle";

const ans = {
	//id : Number(id),
	//available : Boolean(available),
	//count : Number (count),
	//name : String(name),
	//author : String(author)
}
ans.id = Number(id);
ans.available = Boolean(available);
ans.count = Number(count);
ans.name = String(name);
ans.author = String(author);
console.log(ans)